# Port the app listens on (Cloud Run sets this automatically)
PORT=8080

# Optional: GCP project ID (only needed if you want explicit Cloud Logging sink config)
# GOOGLE_CLOUD_PROJECT=your-project-id
